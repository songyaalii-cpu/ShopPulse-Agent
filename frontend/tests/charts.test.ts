import {describe,expect,it} from "vitest";import {splitMetricChart,toEChartsOption} from "../lib/charts";import {reduceEvent} from "../lib/events";
describe("chart contract",()=>{it("preserves immature cohort cells as null",()=>{const o:any=toEChartsOption({chart_id:"c",chart_type:"heatmap",title:"C",x_axis:["M0","M1"],rows:[{cohort:"A",M0:1,M1:null}],metadata:{}});expect(o.series[0].data[1][2]).toBeNull()});it("splits differently scaled metrics into Chinese small-multiple charts",()=>{const panels=splitMetricChart({chart_id:"kpi",chart_type:"line",title:"趋势",x_axis:["2026-08-01"],series:[{name:"gmv",unit:"currency",data:[100000]},{name:"average_order_value",unit:"currency",data:[50]},{name:"refund_rate",unit:"percent",data:[.1]}],metadata:{}});expect(panels.map(panel=>panel.title)).toEqual(["商品交易总额","客单价","退款率"]);expect(panels.every(panel=>panel.series?.length===1)).toBe(true);const refund:any=toEChartsOption(panels[2]);expect(refund.yAxis.axisLabel.formatter(.125)).toBe("12.5%")});it("reduces ordered SSE events",()=>{const e:any={event_id:"2",run_id:"r",sequence:2,event_type:"run.completed",timestamp:"",payload:{}};const state=reduceEvent({events:[],status:"running"},e);expect(state.status).toBe("completed")})});
