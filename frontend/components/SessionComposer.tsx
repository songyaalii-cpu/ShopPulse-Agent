"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {api} from "../lib/api";
export default function SessionComposer({sessionId}:{sessionId:string}){const [query,setQuery]=useState("");const [error,setError]=useState("");const [submitting,setSubmitting]=useState(false);const router=useRouter();async function submit(event:FormEvent){event.preventDefault();setError("");setSubmitting(true);try{const run:any=await api(`/api/v1/sessions/${sessionId}/runs`,{method:"POST",headers:{"Idempotency-Key":crypto.randomUUID()},body:JSON.stringify({query,llm_enabled:true})});router.push(`/runs/${run.run_id}`)}catch(error){setError(String(error))}finally{setSubmitting(false)}}return <section className="composer"><div><h2>继续这个分析主题</h2><p>新的问题会保留在当前会话中，方便连续分析和回看。</p></div><form onSubmit={submit}><textarea value={query} onChange={event=>setQuery(event.target.value)} placeholder="继续追问，例如：请按渠道拆解刚才的变化" required maxLength={2000}/><button className="primary-action" disabled={submitting}>{submitting?"正在创建分析…":"提交追问"}</button></form>{error&&<p className="error">{error}</p>}</section>}
